# Supabase Configuration
# Get these values from your Supabase project settings -> API
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=sb-service-role-key-here
SUPABASE_JWT_SECRET=your-jwt-secret-here

# Cookie Security
# Generate a strong random string (64 chars) for COOKIE_SECRET
# Example: openssl rand -base64 48
COOKIE_SECRET=your-64-character-random-string-here
# Set to your production domain or localhost for development
COOKIE_DOMAIN=localhost|your-domain.com

# Instructions:
# 1. SUPABASE_URL: Found in Project Settings -> API -> Config
# 2. SUPABASE_SERVICE_ROLE_KEY: Found in Project Settings -> API -> Service Role Keys
# 3. SUPABASE_JWT_SECRET: Found in Project Settings -> API -> JWT Settings
# 4. COOKIE_SECRET: Generate using: openssl rand -base64 48
